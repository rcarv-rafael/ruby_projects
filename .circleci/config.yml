version: 2
jobs:
  build:
    docker:
      - image: circleci/ruby:latest


    steps:
      - checkout
      - run: cd  BackEnd
      - run: gem list
      - run: gem install bundler
      - run: bundle
      - run: cucumber --format html --out report.html

